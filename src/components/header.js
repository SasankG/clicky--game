import React from  "react";


//header has no properties it is just an jsx element
const Header = () => (
    <div className ="jumbotron">
        <h1 className="text-center">Click on the images to score points!, but avoid clicking on the same one!</h1>
    </div>
)

export default Header;